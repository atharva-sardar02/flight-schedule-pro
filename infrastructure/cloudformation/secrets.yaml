AWSTemplateFormatVersion: '2010-09-09'
Description: 'Flight Schedule Pro - Secrets Manager for API Keys'

Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues:
      - dev
      - staging
      - production
    Description: Environment name
  
  ProjectName:
    Type: String
    Default: flight-schedule-pro
    Description: Project name for resource naming

  OpenWeatherMapApiKey:
    Type: String
    NoEcho: true
    Description: OpenWeatherMap API Key

  WeatherApiComKey:
    Type: String
    NoEcho: true
    Description: WeatherAPI.com API Key

  OpenAIApiKey:
    Type: String
    NoEcho: true
    Description: OpenAI ChatGPT API Key

Resources:
  # OpenWeatherMap API Key Secret
  OpenWeatherMapSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub '${ProjectName}/${Environment}/openweathermap-api-key'
      Description: OpenWeatherMap API Key
      SecretString: !Ref OpenWeatherMapApiKey
      Tags:
        - Key: Project
          Value: !Ref ProjectName
        - Key: Environment
          Value: !Ref Environment

  # WeatherAPI.com API Key Secret
  WeatherApiComSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub '${ProjectName}/${Environment}/weatherapi-com-key'
      Description: WeatherAPI.com API Key
      SecretString: !Ref WeatherApiComKey
      Tags:
        - Key: Project
          Value: !Ref ProjectName
        - Key: Environment
          Value: !Ref Environment

  # OpenAI API Key Secret
  OpenAIApiKeySecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub '${ProjectName}/${Environment}/openai-api-key'
      Description: OpenAI ChatGPT API Key
      SecretString: !Ref OpenAIApiKey
      Tags:
        - Key: Project
          Value: !Ref ProjectName
        - Key: Environment
          Value: !Ref Environment

  # IAM Role for Rotation Function - Created first to break circular dependency
  RotationFunctionRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: SecretsManagerRotation
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - secretsmanager:DescribeSecret
                  - secretsmanager:GetSecretValue
                  - secretsmanager:PutSecretValue
                  - secretsmanager:UpdateSecretVersionStage
                  - secretsmanager:TestSecretVersion
                Resource: !Sub 'arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:${ProjectName}/${Environment}/database-credentials-*'
              - Effect: Allow
                Action:
                  - rds:ModifyDBInstance
                  - rds:DescribeDBInstances
                Resource: '*'

  # Rotation Lambda Function (for database credentials)
  RotationFunction:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-secret-rotation'
      Runtime: python3.9
      Handler: index.lambda_handler
      Role: !GetAtt RotationFunctionRole.Arn
      Timeout: 30
      Environment:
        Variables:
          SECRETS_MANAGER_ENDPOINT: !Sub 'https://secretsmanager.${AWS::Region}.amazonaws.com'
      Code:
        ZipFile: |
          import boto3
          import json
          import os
          
          def lambda_handler(event, context):
              # Placeholder rotation function
              # In production, implement actual rotation logic
              return {
                  'statusCode': 200,
                  'body': json.dumps('Rotation function placeholder')
              }

  # Database Credentials Secret
  # Note: Rotation is configured after creation to avoid circular dependency
  DatabaseCredentialsSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Name: !Sub '${ProjectName}/${Environment}/database-credentials'
      Description: Database master credentials
      GenerateSecretString:
        SecretStringTemplate: '{"username": "postgres"}'
        GenerateStringKey: 'password'
        PasswordLength: 32
        ExcludeCharacters: '"@/\'
      Tags:
        - Key: Project
          Value: !Ref ProjectName
        - Key: Environment
          Value: !Ref Environment

Outputs:
  OpenWeatherMapSecretArn:
    Description: OpenWeatherMap API Key Secret ARN
    Value: !Ref OpenWeatherMapSecret
    Export:
      Name: !Sub '${ProjectName}-${Environment}-OpenWeatherMapSecretArn'

  WeatherApiComSecretArn:
    Description: WeatherAPI.com API Key Secret ARN
    Value: !Ref WeatherApiComSecret
    Export:
      Name: !Sub '${ProjectName}-${Environment}-WeatherApiComSecretArn'

  OpenAIApiKeySecretArn:
    Description: OpenAI API Key Secret ARN
    Value: !Ref OpenAIApiKeySecret
    Export:
      Name: !Sub '${ProjectName}-${Environment}-OpenAIApiKeySecretArn'

  DatabaseCredentialsSecretArn:
    Description: Database Credentials Secret ARN
    Value: !Ref DatabaseCredentialsSecret
    Export:
      Name: !Sub '${ProjectName}-${Environment}-DatabaseCredentialsSecretArn'


